import { Api } from "../tl";
import type { ValueOf } from "../define";
import { DEFAULT_DELIMITERS, messageEntities } from "../client/messageParse";
export declare class MarkdownParser {
    static parse(message: string, delimiters?: {
        [key: string]: messageEntities;
    }): [string, ValueOf<typeof DEFAULT_DELIMITERS>[]];
    static unparse(text: string, entities: Api.TypeMessageEntity[] | undefined, delimiters?: {
        [key: string]: messageEntities;
    }): string;
}
