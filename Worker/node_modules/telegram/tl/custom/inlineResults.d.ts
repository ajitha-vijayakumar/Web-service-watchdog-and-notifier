import type { TelegramClient } from "../../client/TelegramClient";
import type { EntityLike } from "../../define";
import { Api } from "../api";
export declare class InlineResults<InlineResult> extends Array<InlineResult> {
    private result;
    private queryId;
    private cacheTime;
    private _validUntil;
    private users;
    private gallery;
    private nextOffset;
    private switchPm;
    constructor(client: TelegramClient, original: Api.messages.TypeBotResults, entity?: EntityLike);
    resultsValid(): boolean;
}
