import type { TelegramClient } from "../../client/TelegramClient";
import { Api } from "../api";
import type { Entity } from "../../define";
export declare class Dialog {
    private _client;
    private dialog;
    private pinned;
    private folderId;
    private archived;
    private message;
    private date;
    private entity;
    private inputEntity;
    private id?;
    private name?;
    private title?;
    private unreadCount;
    private unreadMentionsCount;
    private draft;
    private isUser;
    private isGroup;
    private isChannel;
    constructor(client: TelegramClient, dialog: Api.Dialog, entities: Map<number, Entity>, message: Api.Message);
}
